# omni
Omni is a fast, opinionated, rootless package manager that installs tools into an isolated user-space store and runs them in lightweight sandboxes. Packages are distributed as OCI images, keeping your base OS clean while remaining reproducible, portable, and easy to use.
